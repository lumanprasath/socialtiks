import {Component, ViewEncapsulation} from '@angular/core';

@Component({
  selector: 'gamedetails',
  encapsulation: ViewEncapsulation.None,
  template: require('./gamedetails.html'),
})
export class GameDetails {

  constructor() {
  }
}
import { OnInit, ElementRef, Renderer } from '@angular/core';
import { DomSanitizer } from '@angular/platform-browser';
import { CloudData, CloudOptions } from './cloud.interfaces';
export declare class TagCloudComponent implements OnInit {
    private el;
    private renderer;
    private sanitizer;
    data: [CloudData];
    width?: number;
    height?: number;
    removeOverflow?: boolean;
    alreadyPlacedWords: any[];
    options: CloudOptions;
    constructor(el: ElementRef, renderer: Renderer, sanitizer: DomSanitizer);
    ngOnInit(): void;
    drawWordCloud(): void;
    hitTest(currentEl: any, otherEl: any[]): boolean;
    overlapping(a: any, b: any): boolean;
    drawWord(index: any, word: any): void;
}
